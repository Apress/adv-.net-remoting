For this example to work as expected, you have to create a virtual directory in IIS which is called VersionedSAO and which points to the vroot\ subdirectory.

You'll also have to put the VersionedSAO.DLL from Server1.0.0.1\bin\debug and Server2.0.0.1\bin\debug in the GAC.
